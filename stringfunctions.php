<?php

    $name = "Stefan Ward <br>";
    $phone = "1234-567-89";
    $foods = array("rice", "peas", "pie");

    // $name = strtolower($name);
    // $name = strtoupper($name);
    // $name = trim($name);
    // $name = str_pad($name, 20, "3");
    // $phone = str_replace("-", "$", $phone);
    // $name = strrev($name);
    // $name = str_shuffle($name);
    // $equals = strcmp($name, "Stefan Ward");
    // $count = strlen($phone);
    // $index = strpos($phone, "-");
    // $firstname = substr($name, 0, 6);
    // $lastname = substr($name, 6);
    // $fullname = explode(" ", $name);
    // $foods = implode("-", $foods);

    // echo $foods;
    // echo  $name;
    // echo  $equals;
    // echo  $phone;
    // echo $count;
    // echo $index;
    // echo $firstname;
    // echo $fullname[1];
    
?>